apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: ml-service-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/ml-service-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/ml-service-default: "true"
